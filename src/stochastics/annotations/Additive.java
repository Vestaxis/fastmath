package stochastics.annotations;

import stochastic.measures.Measure;

/**
 * 2. (of a set function on a class of sets) distributing over addition, so
 * that, for disjoint sets whose union is in the class, f(A∪B) = f(A) + f(B).
 * See also {@link Measure}
 * 
 * @author crow
 *
 */
public @interface Additive
{

}
