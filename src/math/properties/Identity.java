package math.properties;

import math.theory.group.Group;

/**
 * an identity element (or neutral element) is a special type of element of a
 * set with respect to a binary operation on that set. It leaves other elements
 * unchanged when combined with them. This is used for {@link Group}s and
 * related concepts.
 * 
 * @author crow
 *
 */
public @interface Identity
{

}
