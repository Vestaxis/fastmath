package math.theory.ring;

/**
 * A semiring is an algebraic structure similar to a ring, but without the
 * requirement that each element must have an additive inverse. Wikipedia claims
 * that the term rig is also used occasionally — and that this originated as a
 * joke, suggesting that rigs are rings without negative elements.
 */
public interface SemiRing
{

}
