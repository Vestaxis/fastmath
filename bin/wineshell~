#!/bin/sh 

FMDIR=$(dirname $(readlink -f $0))/..
cd $FMDIR
~/c/jdk9/bin/jshell.exe -J-Djava.library.path=$(winepath -w ..) --class-path=`$FMDIR/bin/echoWineClasspath` --startup=$(winepath -w $FMDIR/shell.startup)

